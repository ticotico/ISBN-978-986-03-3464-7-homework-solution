//寫一個程式將一個數列一奇偶順序印出。輸入的第一行為數列的長度 n，
//第二行為數列中的數字。輸出有兩行，第一行為數列中的第一個、第三個
//、第五個等數字，第二行為第二個、第四個、第六個等數字。同一行輸出
//的數字將有一個空格。
//參數規範: 0 < n <= 100

#include <stdio.h>
main()
{
  int a[101], n, i;
  scanf("%d", &n);
  for (i = 0; i < n; i++)
    scanf("%d", &a[i]);

  for(i = 0; i < n; i += 2)
    printf("%d ", a[i]);
  printf("\n");
  for (i = 1; i < n; i += 2)
    printf("%d ", a[i]);
  printf("\n");
}
